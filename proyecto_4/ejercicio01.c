#include <stdio.h>
#include <stdbool.h>
#include <assert.h>

void holaHasta(int n)
{
}

int main(void)
{
    int x;
    printf("Ingrese un valor numérico entero\n");
    scanf("%d", &x);
    assert(x > 0);
}